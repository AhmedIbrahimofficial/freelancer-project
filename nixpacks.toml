[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo_mysql", "php83Extensions.mbstring", "php83Extensions.bcmath", "php83Extensions.gd", "php83Extensions.curl", "php83Extensions.zip", "php83Extensions.intl", "php83Extensions.exif", "php83Extensions.fileinfo", "php83Extensions.xml", "php83Extensions.tokenizer", "composer"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader --no-interaction --no-scripts --ignore-platform-reqs"
]

[phases.build]
cmds = [
  "composer dump-autoload --optimize",
  "php artisan package:discover --ansi || true",
  "php artisan config:cache",
  "php artisan route:cache",
  "php artisan view:cache"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"
